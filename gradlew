#!/usr/bin/env bash
# Placeholder gradlew script: this repo provides GitHub Actions workflow to build.
echo "You can build using local gradle or use the provided GitHub Actions CI to build the APK."
exit 1
